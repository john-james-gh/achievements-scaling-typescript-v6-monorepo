{
  "name": "@achievements-scaling-typescript-v6-monorepo/root",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "format": "prettier . --write && sort-package-json package.json apps/*/package.json libs/*/package.json",
    "format:check": "prettier . --check && sort-package-json package.json apps/*/package.json libs/*/package.json --check",
    "prune:dist": "pnpm --recursive exec rm -rf dist",
    "prune:modules": "pnpm --recursive exec rm -rf node_modules"
  },
  "devDependencies": {
    "prettier": "^3.8.4",
    "sort-package-json": "^4.0.0"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.9.0",
      "onFail": "download"
    },
    "runtime": {
      "name": "node",
      "version": "24",
      "onFail": "warn"
    }
  }
}
